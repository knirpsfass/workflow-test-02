name: test

inputs:
  test:
    description: abc
    default: '["abc", "eee"]'

env:
  def: 'abc'

runs:
  using: composite
    
  steps:
    - name: echoooo
      shell: bash
      run: |
        echo "$INPUT_ARRAY" | jq -r '.[]' | while IFS= read -r element; do
          echo "Element: $element"
        done
