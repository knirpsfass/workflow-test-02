name: lol
on:
  workflow_dispatch:
    inputs:
      test:
        type: boolean
        description: Include 🎉🤣 emojis
        default: true

jobs:
  build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: aaaaaa
        uses: knirpsfass/workflow-test-02/.github/action@main
        with:
          test: ${{ inputs.abc }}
