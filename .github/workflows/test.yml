name: lol
on:
  workflow_dispatch:
    inputs:
      test:
        type: boolean
        description: Include 🎉🤣 emojis
        default: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "acceptance tests"
